class Math {
    static square(n) {
        return n * n;
    }
    sum(a, b){
        return a + b;
    }
}

/*
static fun test() {
    print "bad";
}

test();
*/

var math = Math();
print math.sum(5, 9);

print Math.square(2);

